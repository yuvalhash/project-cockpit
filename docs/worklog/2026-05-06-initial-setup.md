# Work Log: Initial Setup

## Intent

Create the first version of `Project Cockpit` with a minimal app scaffold and the tracking structure for logs, todos, architecture, and memory.

## Changes

- Added a static app scaffold in `index.html` and `src/`
- Added architecture notes in `docs/architecture/`
- Added task pipeline folders in `docs/todos/`
- Added memory files in `memory/`

## Verification

- Confirmed the workspace layout
- Checked that the scaffold files exist and are readable
- Kept the app dependency-free so it can open directly in a browser

## Next

- Add task creation and log templates
- Decide whether the next version stays static or moves to a small app framework

## V2 Direction

- Focus the project on scraping specific websites
- Extract structured data into file-based snapshots
- Generate LLM outputs from changes or matched conditions
