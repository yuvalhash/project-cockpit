# GitHub Remote Repair ToDo

- [ ] Create `yuvalhash/project-cockpit` on GitHub or grant the connected account access.
- [ ] Install or authenticate `gh`, if CLI-based repository creation or PR creation is needed.
- [ ] Re-run repository creation from Codex if a create-repository tool or authenticated `gh` becomes available.
- [ ] Verify `git ls-remote --heads origin main` succeeds.
- [ ] Push `main` with upstream tracking using `git push -u origin main`.
- [ ] Confirm the remote branch is visible from GitHub.
