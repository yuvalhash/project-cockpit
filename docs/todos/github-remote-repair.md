# GitHub Remote Repair ToDo

- [x] Create `yuvalhash/project-cockpit` on GitHub or grant the connected account access.
- [ ] Install or authenticate `gh`, if CLI-based repository creation or PR creation is needed.
- [x] Skip Codex-side repository creation because the repository was created manually.
- [x] Verify `git ls-remote --heads origin main` succeeds.
- [x] Push `main` with upstream tracking using `git push -u origin main`.
- [x] Confirm the remote branch is visible from GitHub.
